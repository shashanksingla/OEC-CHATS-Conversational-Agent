---
topic: Provider conversation context and server-owned continuation projections
updated: 2026-09-10T11:57
---

- (capability) CAP-1: Provider-facing composite results expose server-owned opaque continuation projections for attendance and payment follow-ups; success requires action inputs contain only opaque context and action references and the server resolves the validated plan.
- (capability) CAP-2: Compatible attendance and payment continuations reuse provider-bound canonical results before new source retrieval; success requires matching immutable compatibility fields to render the requested projection, including pagination where supported.
- (capability) CAP-3: Continuation requests support explicit refresh and bounded lifecycle behavior; success requires refresh bypasses canonical-result reuse, returns a new context, and TTL, LRU entry, and byte bounds prevent unbounded continuation storage.
- (capability) CAP-4: Provider-facing MCP results separate prose from compact routing metadata; success requires content[0].text to be the sole provider-facing response and structuredContent to contain only compact status, scope, freshness/pagination metadata, and opaque controls.
- (constraint) Continuation state is server-owned and process-local; the model must not supply cached datasets, child lists, raw identifiers, or duplicated response text.
- (constraint) Compatibility reuse requires an immutable normalized key covering authenticated provider scope, capability, normalized date scope and filters, focus/view, rule version, freshness policy, projection type, page, and page size.
- (constraint) When continuation fields are present, malformed, unknown, expired, provider-mismatched, capability-mismatched, or incompatible references fail closed and never fall back to model-supplied direct filters.
- (constraint) Cross-capability actions preserve the originating provider and county scope; the target capability performs its own authorization and compatibility validation.
- (constraint) Continuation storage uses provider-scoped process memory with TTL, LRU entry, and byte caps; it is not durable storage and process restart clears it.
- (constraint) Existing direct attendance and payment inputs remain supported, and deterministic calculation ownership stays in the existing evaluators.
- (assumption) The existing implementation is the runtime baseline; requirements observed in the finalized architecture are treated as intended contract rules, while unverified enforcement remains an open question or implementation constraint.
- (question) Does the runtime implement one immutable normalized compatibility key across both attendance and payment, including normalized scope, filters, focus/view, rule version, freshness, projection, page, and page size?
- (question) Does explicit refresh bypass both the conversation-context result cache and the lower-level source-read cache, or only the former?
- (question) Are malformed or partially supplied continuation references rejected even when direct date, view, or filter inputs are also present?
- (question) Are inherited cross-capability actions bound to a still-valid originating context and compatibility key rather than only the provider and action ID?
- (question) Does the byte bound cover session action metadata as well as canonical context records, and does pagination reuse a compatible cached projection or always trigger fresh retrieval?
- (event) Architecture source reconciled against current implementation; runtime gaps recorded as open questions rather than unsupported behavior.
- (event) Pass 1 coherence validation passed: CAP-1 through CAP-4 each contain intent and success; constraints bend design; non-goals are explicit; success is testable; IDs are stable; prose is lean.
- (event) Pass 2 preservation validation passed: finalized architecture continuation rules, legacy contract behavior, and observed implementation gaps are represented in SPEC.md or runtime-gaps.md; wrapper-only ceremony was omitted.
- (event) Focused validation recorded: conversation-context and protocol tests reported 141 passed and 1 failed because an existing attendance provider-text assertion expected sections absent from the current formatter; no runtime code was changed.
