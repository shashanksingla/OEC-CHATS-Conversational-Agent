---
scope: Provider Assist runtime ownership and maintainability
purpose: Compact architecture spine plus maintainer and developer cleanup map
altitude: feature
updated: 2026-09-10T11:42
---

- (decision) [ADOPTED] Use a layered, capability-oriented architecture: agent interpretation, minimal skills, MCP boundary/orchestration, Apex source retrieval, TypeScript normalization, and Python deterministic evaluation. This prevents business logic from drifting into prompts or transport code.
- (decision) [ADOPTED] The authenticated provider scope is established inside MCP and Apex; model input may select capability and bounded filters but never provider identity, authorization scope, or unrestricted records. This prevents cross-provider access and identity drift.
- (decision) [ADOPTED] Each provider-facing action has one composite MCP capability owner. The general server registers and dispatches tools, but attendance, confirmation, absence-limit, and payment orchestration live in domain modules. This prevents duplicated fan-out and competing formatters.
- (decision) [ADOPTED] TypeScript owns transport, validation, scope injection, source mapping, normalization, orchestration, and provider-safe result shaping; Python owns deterministic rules, date logic, counts, classifications, and money calculations. This prevents arithmetic and source joins from being reimplemented in multiple layers.
- (decision) [ADOPTED] Apex is an authoritative read-only source and secondary authorization guard. It retrieves Salesforce records and preserves source relationships; it does not interpret conversational intent or calculate provider-facing risk.
- (decision) [ADOPTED] Canonical contracts are explicit at the MCP-to-evaluator boundary; identifiers, dates, money, freshness, provenance, capability, and error status are structured. Unknown or ambiguous source fields fail closed rather than being inferred.
- (decision) [ADOPTED] Generated output is never hand-edited and is not a source of truth: mcp/cccap-provider-api/dist is rebuilt from src, and skills/reports/eval-runs is historical evaluation output. Runtime source remains .github/agents, skills, mcp/src, Python scripts, and CHATS_SIT Apex.
- (assumption) [ASSUMPTION] The first maintainability pass should reorganize documentation and ownership before physically moving runtime modules; current green tests and MCP registration make a map-first refactor lower risk.
- (decision) [ADOPTED] Current identity scope is initialized once per MCP process from the Salesforce CLI authenticated user; production Apex-side identity derivation is explicitly deferred. This prevents local process behavior from being mistaken for the production authorization design.
- (decision) [ADOPTED] Every capability must expose one request entrypoint, one canonical input adapter, one evaluator invocation, and one provider-safe result contract. MCP schemas, TypeScript normalizers, and Python evaluators own validation at their respective boundaries.
- (event) spine finalized; lint passed; MCP tests 61 passed; Python unittest tests 69 passed; pytest unavailable in the configured interpreter
- (decision) [ADOPTED] Define one server-owned continuation projection protocol for attendance and payment: an opaque action reference resolves to a validated capability, scope, filters, focus, view, and pagination plan; the model never owns continuation state or resends cached data. Binds: all composite follow-ups. Prevents: replayed summaries, divergent attendance/payment continuation behavior, and model-built filters.
- (decision) [ADOPTED] Use cache-first projection reuse: serve a continuation from the provider-bound canonical result when capability, provider scope, rule version, freshness, requested projection, and pagination are compatible; bypass cache for explicit refresh, expiry, incompatible scope/rule version, or missing projection data. Binds: cache reads and refresh behavior. Prevents: unnecessary source calls and stale or incorrectly scoped detail.
- (decision) [ADOPTED] Make `content[0].text` the sole provider-facing response channel; structuredContent contains only compact status, scope, pagination, and opaque action controls, never canonical rows, child lists, raw identifiers, or duplicated provider prose. Binds: MCP response envelope and agent relay. Prevents: oversized handoffs and competing response sources.
- (question) Define the final cache compatibility key across provider identity, capability, normalized scope, filters, focus/view, rule version, freshness, and requested projection/page; current coaching decision leaves this open pending implementation review.
- (decision) [ADOPTED] Continuation compatibility is evaluated against an immutable normalized key containing provider scope, capability, date scope, filters, focus/view, rule version, freshness policy, projection type, page, and page size; a mismatch cannot reuse cached canonical data. Binds: cache lookup and pagination. Prevents: stale or cross-scope detail pages.
- (decision) [ADOPTED] Invalid, expired, malformed, or capability-mismatched continuation references fail closed and never fall back to model-supplied direct inputs when continuation fields are present. Binds: action resolution and error handling. Prevents: accidental scope widening and stale-action replay.
- (decision) [ADOPTED] Cross-capability actions carry only opaque references, but the resolved originating scope remains bounded and the target capability performs its own authorization and compatibility validation before retrieval. Binds: attendance-to-payment and payment-to-attendance actions. Prevents: inherited actions widening provider or county scope.
- (decision) [ADOPTED] Continuation storage uses bounded provider-scoped process memory with the existing TTL, LRU entry cap, and byte cap; explicit refresh bypasses canonical-result reuse and creates a new compatible context. Binds: resource lifecycle and freshness. Prevents: unbounded memory, stale reads, and refresh no-ops.
- (event) architecture amendment reviewer gate completed; lint passed; current-reality and adversarial reviewers identified implementation gaps for the next build pass.
- architecture amendment finalized; continuation projection invariants, compatibility, fail-closed resolution, cross-capability scope, and bounded refresh lifecycle recorded; lint passed with 0 findings
